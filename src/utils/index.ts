/**
 * Barrel export for all utility functions.
 * Consumers import from '@/utils' — not individual files.
 *
 * @example
 * import { cn, formatDate, getInitials } from '@/utils'
 */

export * from './cn'
export * from './format'
export * from './debounce'
